SQLite database created from the marketing events dataset.

The database was prepared using Python and Pandas and was used for SQL-based data extraction, aggregation, segmentation, and root-cause analysis in DB Browser for SQLite.
